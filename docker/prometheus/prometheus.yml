global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "backend-symfony"
    scheme: https
    metrics_path: /api/metrics/prometheus
    tls_config:
      insecure_skip_verify: true
    static_configs:
      - targets:
          - nginx:443
